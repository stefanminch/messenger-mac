// Preload script runs before the web page loads
// Using contextIsolation for security

window.addEventListener('DOMContentLoaded', () => {
  // App is ready
  console.log('Messenger app loaded');
});
